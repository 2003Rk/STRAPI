{
  "name": "firestore-sync-function",
  "version": "1.0.0",
  "description": "Cloud Function to sync Strapi events to Firestore",
  "main": "index.js",
  "scripts": {
    "start": "functions-framework --target=firestoreSync",
    "deploy": "gcloud functions deploy firestore-sync --gen2 --runtime nodejs20 --region asia-south1 --entry-point firestoreSync --trigger-topic strapi-events"
  },
  "dependencies": {
    "@google-cloud/firestore": "^7.10.0",
    "@google-cloud/functions-framework": "^3.0.0"
  },
  "engines": {
    "node": ">=18"
  },
  "author": "",
  "license": "ISC"
}
